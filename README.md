# Decision-Tree-Script
A fully command-line executable Decision Tree Classifier built in Python using scikit-learn, demonstrating predictive machine learning without GUI dependencies.  Detailed Description (For the submission portal) This project is a standalone command-line interface (CLI) machine learning application.
